bundle:
  name: dbx_poc

workspace:
  # Do NOT set host here — Databricks forbids interpolation
  root_path: /Workspace/Repos/ankit.af.yadav@accenture.com/dbx_poc

targets:
  dev:
    workspace:
      root_path: /Workspace/Repos/ankit.af.yadav@accenture.com/dbx_poc

resources:
  jobs:
    poc_ingest_job:
      name: poc_ingest_job
      tasks:
        - task_key: run_notebook
          notebook_task:
            notebook_path: notebooks/poc_notebook
